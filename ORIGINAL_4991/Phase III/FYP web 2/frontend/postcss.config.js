// frontend/postcss.config.js
module.exports = {
    plugins: {
      tailwindcss: {},
      autoprefixer: {},
      // Add other PostCSS plugins here if needed, but usually not required for base setup
    },
  }